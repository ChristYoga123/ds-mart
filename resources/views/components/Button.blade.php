@props(['type' => 'submit', 'text'])
<button type="{{ $type }}"
    class="w-full py-2 px-4 bg-gray-900 hover:bg-gray-800 text-white font-semibold rounded-md shadow transition cursor-pointer">{{ $text }}</button>
